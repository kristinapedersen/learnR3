---
title: "Draft R Markdown document"
author: "Your Name"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(vroom)
library(here)
library(fs)
library(tidyverse)
source(here("R/functions.R")) #runs all the functions I have make at once
load(here("data/mmash.rda"))
```

## Pivotting longer

```{r}
mmash %>%
    select(-samples) %>%
    pivot_longer(c(-user_id, -day, -gender)) %>%
    group_by(gender, day, name) %>%
    summarise(across(value, list(Mean = mean, SD = sd), na.rm = TRUE)) %>%
    ungroup()
```

